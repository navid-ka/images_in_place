#:import rgba kivy.utils.rgba

<VBoxWidget>:
    filename: filename
    # ---------- Display Images----------
    BoxLayout:
        Image:
            id: filename
            source: app.filename
            filter_one: root.filter_one

# ---------- Button Save Load ----------
    
    GridLayout:
        cols: 2
        rows: 4
        orientation: "horizontal"
        size: [1,.25]
        pos: root.pos
        padding:100
        Label:
        Label:
        Button:
            text: "Upload Image"
            background_color: rgba('A94A64')
            on_press: root.get_image_one()

        Button:
            text: "Save"
            background_color: rgba('A94A64')
            #on_press: root.save_image()
        Button:
            id: filter_one
            text: "Darker"
            background_color: rgba('A94A64')
            on_press: root.filter_one(root.filename.source); print(root.filename.source)
        Button:
            text: "Lighter"
            background_color: rgba('A94A64')
            #on_press: 

        Widget: