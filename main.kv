#:import rgba kivy.utils.rgba
#:import Factory kivy.factory.Factory

<FC@FileChooserListView>:
    on_selection:
        print(self.selection)
    on_submit:
        app.filename = self.selection[0]
        self.parent.dismiss()
<CustomPopUp@ModalView>:
    FC:

<VBoxWidget>:
    image: image
    # ---------- Display Images----------
    BoxLayout:
        Image:
            id: image
            source: app.filename
    GridLayout:
        cols: 2
        rows: 4
        orientation: "horizontal"
        size: [1,.25]
        pos: root.pos
        padding:100
            # ---------- Button Save Load ----------
        Button:
            id: btn_popup
            text: "Upload Image"
            background_color: rgba('A94A64')
            on_release:
                self.popup = Factory.CustomPopUp()
                self.popup.open()

        Button:
            text: "Save"
            background_color: rgba('A94A64')
            #on_press: root.save_image()
        # ---------- Filters ----------
        Label:
        Label:
        Button:
            id: filter_one
            text: "Darker"
            background_color: rgba('A94A64')
            on_press: 
                root.filter_one(root.image) == True
                root.image.reload()
        Button:
            id: filter_two
            text: "Lighter"
            background_color: rgba('A94A64')
            on_press: root.filter_two(image) == False

        Widget: